import { View } from "react-native"
import MapView from "react-native-maps"

export const MapConp = () => {
  return (
   <View>
     <MapView style={{flex: 1, width: "500", height:"200"}}
      
      >
    
      </MapView>
   </View>
  )
}
